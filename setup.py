from setuptools import setup

setup(
   name='britnicanalewebsite',
   version='1.0',
   description='A personal website',
   author='Britni Canale',
   author_email='britnicanale@gmail.com',
   packages=['britnicanalewebsite'],
   install_requires=['flask', 'os'],
)